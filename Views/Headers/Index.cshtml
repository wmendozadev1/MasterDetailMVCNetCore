@model IEnumerable<TestMVCNetCore.Models.Header>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Header_Comment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Header_Comment)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Header_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Header_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Header_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
